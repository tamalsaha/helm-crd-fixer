apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubedb-crd-installer.fullname" . }}
  labels:
    {{- include "kubedb-crd-installer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubedb-crd-installer.selectorLabels" . | nindent 4 }}
