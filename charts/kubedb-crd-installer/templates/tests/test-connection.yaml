apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubedb-crd-installer.fullname" . }}-test-connection"
  labels:
    {{- include "kubedb-crd-installer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubedb-crd-installer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
